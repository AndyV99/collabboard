# Architecture Decision Records

One file per decision, `NNNN-short-title.md`, numbered sequentially and never
renumbered. Use the `write-adr` skill so the format stays consistent.

A decision belongs here when it is expensive to reverse — a datastore choice, a
messaging pattern, a cloud service picked over an alternative. Reasoning that
lives only in chat history is lost reasoning.
